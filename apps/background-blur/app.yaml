schema: 1
name: background-blur
title: Depth-aware Background Blur
commands: [bg-blur]
remote:
  entrypoint: background_blur.py
  default_config: config.yaml
  gpu: true
artifacts:
  report: report.html
  metadata: metadata.json
